# CSO Security Sign-Off — Issue #24 (ORIGINAL_DESIGN Site Generation)

**Date:** 2026-09-01
**Scope:** Diff since `39d4018`: `src/domain/original-design.ts` (new), `src/domain/lifecycle.ts` (mode gate), `src/domain/visual-blueprint.ts` (optional `sourceTraitId` + validator hardening), `tests/v2-original-design.test.ts`, hardening of `tests/v2-proof-gate.test.ts` + `tests/v2-benchmark-harness.test.ts` (FK-safe real entities; case-scoped suite assertion).
**Reviewer:** AI-assisted CSO pass (morabeza-cso skill).

## 1–3. Scope / attack surface
New AI stage and a product-mode authorization gate. No new public routes, secrets, or visitor input. Sensitive aspects: mode-gate bypass resistance and the loosened→re-tightened blueprint schema boundary.

## 4. Findings

### Verified properties (no finding)
- **Mode gate:** `createInitialBuild` is the single boundary every caller uses (route and workflow tests both go through it); ORIGINAL_DESIGN builds throw while the proof gate is shut and proceed when open — test-proven in both directions. No route creates builds by another path.
- **Schema boundary integrity:** making `sourceTraitId` optional does not weaken REFERENCE_BOUND — the identity-preservation validator now explicitly fails traits without a Reference Analysis trace (test-proven), while ORIGINAL_DESIGN legitimately omits traces; archetype determinism is structurally unrepresentable (`additionalProperties: false`, no preset field) and additionally linted.
- **Downstream reuse without duplication:** the same contract planner, generator, validators, artifact kinds and state machine serve both modes — verified by identical workflow-event sequences and artifact kinds; no second architecture was introduced.
- **Input handling:** CreativeDirection is schema-bounded text flowing into prompts only; outputs pass the same AI boundary (versioned schema, one targeted repair, provenance persistence).

### W13 — Gate check timing (Watch item, Low)
The gate is evaluated at Build creation, not at Site Generation intake; an ORIGINAL_DESIGN submission can be *recorded* while the gate is shut (it just cannot start a Build). This matches the issue wording ("cannot start") — noting it so a future audit doesn't mistake recorded submissions for a bypass.

## 5–7. Severity / remediation / watch items
Critical 0 · High 0 · Medium 0 · Low 1 (W13). No remediation before advancement. Standing items (M1–M3, L2–L3, W1–W12) unchanged.

## 8. Final security verdict
**SECURITY OK WITH WATCH ITEMS** — issue #24 may advance to its dedicated commit.

## 9. Next best action
Commit issue #24, then fetch and run issue #25 (contract away superseded V1 product architecture) — a deletion-heavy diff that will need the most careful CSO pass of the remaining work.
