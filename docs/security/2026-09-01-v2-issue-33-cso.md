# CSO Security Sign-Off — Issue #33 (Automatable preparation: V2 secret inventory, hygiene tooling, staging sender wiring)

**Date:** 2026-09-01
**Scope:** Working tree vs `059e98c`: `wrangler.test.jsonc` (retired dummy vars removed), `scripts/verify-v2-secrets.mjs` (new, name-only secret hygiene gate), `docs/runbooks/2026-09-01-issue-27-v2-production-deployment-and-secrets-runbook.md` + `docs/runbooks/2026-09-01-v2-production-topology-audit.md` (provider ownership, fresh-vs-reuse guidance, audited secret classification, `WAZIBIZ_SENDER_EMAIL` steps). Operational (no repo artifact): staging D1 `site_configurations.site_stg_smoke_1.sender_identity` reset from the pre-#32 placeholder to `''` (platform default from env).
**Reviewer:** AI-assisted CSO pass (morabeza-cso skill). Not a substitute for an external audit.

## 1. What was audited
The automatable portion of #33: re-audit of the required/retired V2 secret inventory against live source references, removal of retired V1 dummy vars from the test Worker config, a name-only hygiene gate script for the future V2 production Worker, provider-ownership documentation, and the staging wiring that lets `WAZIBIZ_SENDER_EMAIL` drive the real Email Service path.

## 2. Security scope
Secret-name hygiene and configuration boundaries. No secret values were created, read, printed, committed or transmitted at any point (Worker secrets are write-only by platform design; the hygiene script lists names only).

## 3. Attack-surface summary
- `wrangler.test.jsonc` vars reduced to the referenced set; removed names had zero references in `src/`+`tests/` (audited by grep before removal).
- `scripts/verify-v2-secrets.mjs`: read-only `wrangler secret list` wrapper; compares names against required/optional/retired sets; exit 1 on violations; never handles values.
- Staging D1 row update targeted `website_factory_staging` (V2 verification environment) — positively identified as **not** V1 (`website_factory_v1` untouched; V1 Worker's secret list re-observed read-only and unchanged, failing V2 hygiene exactly as a preserved V1 resource should).

## 4. Findings

### Verified properties (no finding)
- **Required-set audit re-confirmed against source:** `ZHIPU_API_KEY`, `OPENROUTER_API_KEY`, `KIE_API_KEY`, `CF_AIG_TOKEN`, `CF_DEPLOY_API_TOKEN`, `OPERATOR_CAPABILITY_SECRET`, `WEBHOOK_SECRET` all have live references; `TURNSTILE_SECRET_KEY` remains optional (referenced, active only when a Site sets `turnstile_required = 1`; staging runs with 0).
- **Retired-set audit re-confirmed:** zero source references for `SMTP2GO_API_KEY` (only a negative-assertion comment in `src/lib/publish.ts` stating no such binding ships), `GITHUB_TOKEN`, `GITHUB_WEBHOOK_SECRET`, `APPROVAL_SECRET`, `CANDIDATE_VALIDATION_SECRET`, `WAZIBIZ_EMAIL_TRANSPORT_TOKEN`, `WAZIBIZ_EMAIL_TRANSPORT_URL`, `EMAIL_ROUTER` — the corrected #28 architecture needs none of them (native `send_email` binding only).
- **Hygiene gate discriminates correctly:** run read-only against the preserved V1 Worker it reports the expected violations (4 retired names present, `OPERATOR_CAPABILITY_SECRET` missing) and exits 1 — proving it will catch a mis-provisioned V2 Worker at the #27 gate while leaving V1 itself untouched.
- **No value disclosure anywhere:** script output is names + presence booleans; docs record names/ownership only.
- **Staging sender wiring:** the smoke Site now resolves `From` from the staging `WAZIBIZ_SENDER_EMAIL` var (issue #32 path) instead of a stale stored placeholder; production remains unset until the operator approves a mailbox.

### F1 — Hygiene script trusts `wrangler` output shape (Low — accepted)
The script parses the JSON array out of `wrangler secret list` output; a future wrangler output change could break parsing (it fails closed with a parse error, never a false "clean"). Acceptable for an operator gate run at deploy time.

### F2 — V1/V2 shared provider credentials (Low — operational, documented)
If the operator reuses V1-era provider values, both products share blast radius and rotation. Documented in the runbook with a fresh-credentials recommendation; deliberate operator choice.

## 5. Severity summary
Critical 0 · High 0 · Medium 0 · Low 2 (F1 parser fragility, fails closed; F2 reuse trade-off documented). No unresolved blocking findings.

## 6. Required remediation
None in code. Remaining #33 items are operator-gated by definition: dashboard Email Sending domain onboarding, production `WAZIBIZ_SENDER_EMAIL` approval, and provider secret provisioning (all prepared as runbook steps + hygiene gate).

## 7. Watch items
- **W-33a (Medium, operational):** the real `delivered` Email Service result — the actual #33 completion criterion — is blocked on the operator actions above; fail-closed staging evidence (`E_SENDER_DOMAIN_NOT_AVAILABLE`) proves the deterministic path only.
- **W-33b (Low):** after domain onboarding, tighten the `send_email` binding with `allowed_sender_addresses` (carried W-28g/W-32b).
- **W-33c (Low):** re-run `scripts/verify-v2-secrets.mjs` as a hard #27 gate step (already inserted into the runbook).

## 8. Final security verdict
**SECURITY OK FOR CURRENT SCOPE** — the automatable #33 preparation may advance to its dedicated commit. No secrets were exposed; retired names are purged from V2 configuration; the hygiene gate is in place for #27.

## 9. Next best action
Commit the #33 preparation, deploy the staging environment with the sender var, run the controlled staging smoke to record the env-sender fail-closed evidence on the real Email Service path, then stop and issue the consolidated operator-action checklist.
