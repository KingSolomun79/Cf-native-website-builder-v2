# QA Sweep — V2 issues #5–#16, #24, #25

**Date:** 2026-09-01 · **Mode:** morabeza-qa pass (post-land, per-issue CSO already done)
**Probes:** `tests/v2-qa-sweep.test.ts` (13 scenarios; findings encoded as `it.fails` tripwires)
**Gates at report time:** 27 files / 166 tests passed · `tsc --noEmit` clean

## 1. What was tested

Fourteen landed issues re-verified against their GitHub acceptance criteria using the primary-seam suites, targeted edge-case probes (asserting *desired* behavior so gaps surface as failures), and cross-issue regression runs on the shared contracted tree. QA posture: find/classify/verify — no product fixes applied during the pass.

## 2. Acceptance criteria results

| Issue | Criteria | Result | Basis |
|---|---|---|---|
| #5 Revision/Fact Update | 7/7 | PASS | `v2-revision.test.ts` + probe: partial socials merge, empty-delta revision, lineage precedence, design-origin rejection, immutability |
| #6 Prompt/schema/provenance | 7/7 | PASS | `v2-prompt-contract.test.ts` (manifest identity, one-repair, terminal-invalid, provenance, immutable artifact); runtime-agreement sweep in `v2-final-verification.test.ts` over ALL `ai_stage_runs` |
| #7 Reference intake | 8/9 | **GAP (F2)** | Suitability outcomes, adaptation-contract enforcement, screenshot authority, immutability, no fact promotion all verified; content validation of screenshot bytes NOT enforced (probe accepted garbage bytes) |
| #8 Analysis/Blueprint/Contract | 8/8 | PASS | Separation/immutability, evidence anchors, identity preservation, reference-content lint, contract boundaries, blockers |
| #9 Four-page generation | 8/8 | **GAP (F3)** | Completeness, shared-contract consistency, responsive integrity, incremental production verified; fabrication lint misses founding-year phrasing |
| #10 Two-wave images | 8/8 | PASS | Waves, ~12-target expansion, hard gate + reserve, retry bounds, slot identity, persistence |
| #11 Form Service | 8/8 | PASS | Probes added: real-site-without-configuration → 404; exact 200/5000-char boundaries accepted, 201/5001 rejected; all prior origin/Turnstile/payload/rate/retry coverage |
| #12 Preflight/Preview | 6/6 | **GAP (F4, low)** | All rejection classes, exact-version deploy, idempotence, supersession verified; JSON-LD check is parse-only (literal `null` passes) |
| #13 Release QA | 8/8 | PASS | Probe added: exactly-90 passes / 89 fails on all three scores; hard gates not averageable; exact-version pinning |
| #14 Automated Repair | 8/8 | PASS | Probe added: BLUEPRINT_REVIEW_REQUIRED escalation also from the Release Blocker Fix path; ceilings, confirmation pinning, degraded/failed |
| #15 Approval/Publication/Rollback | 9/9 | PASS | Probes added: first-publication rollback → `NO_ROLLBACK_VERSION`; approval without assembled manifest → `NOT_RELEASE_READY`; prior drift/retry/window coverage |
| #16 Retention | 8/8 | PASS | Probe added: same-version supersession no-op; failed/superseded cleanup, protection, compact records, benchmark policy |
| #24 ORIGINAL_DESIGN | 7/8 | **GAP (F1)** | Gate, no-Reference dependency, archetype non-determinism, downstream reuse verified; **gate bypassable via Revision Request** (probe created an OD build while the gate was shut) |
| #25 V1 contraction | 8/8 | PASS | Route-table/binding/grep audits (issue CSO), full suite on contracted tree; probe: kie-callback ack issues zero database statements |

## 3. Scenarios tested (probes)

Happy-path re-verification came from the existing 153-test suite; the 13 new probes covered: partial socials fact-merge + empty-delta revision (#5); non-image screenshot bytes (#7); "Established 1998" fabrication phrasing (#9); unconfigured-site form submission + exact field-length boundaries (#11); literal-null JSON-LD (#12); score boundaries 89/90 (#13); blocker-fix blueprint escalation (#14); first-publication rollback + manifest-less approval (#15); same-version supersession (#16); revision-path mode-gate bypass with deterministic gate shut→open→shut (#24); write-free KIE ack via DB-statement spy (#25).

## 4. Findings

- **F1 (Medium, verified, needs rework) — #24 acceptance-criterion gap.** `createRevisionBuild` does not check the proof gate: with the gate shut, a Revision Request on a pre-existing ORIGINAL_DESIGN build starts a new OD Build, violating "ORIGINAL_DESIGN cannot start unless the gate is open". Requires a pre-existing OD build (gate was open at least once), so not a cold-start bypass. Fix shape: gate check in `createRevisionBuild` when the parent's Site Generation is ORIGINAL_DESIGN; tripwire QA-F1 flips automatically.
- **F2 (Medium, verified, needs rework) — #7 screenshot content unvalidated.** `readScreenshotBytes` accepts arbitrary bytes (extension-based MIME only); garbage is frozen as the canonical Reference Screenshot and flows into evidence/vision. The retained infra (`reference-input.validateScreenshot` with PNG magic/size checks) exists but is unwired. Fix shape: validate before freezing; add `REFERENCE_SCREENSHOT_INVALID` code; tripwire QA-F2.
- **F3 (Medium, verified, needs rework) — #9 fabrication lint gap.** `FABRICATED_YEAR` only matches `since 19xx/20xx`; "Established 1998", "Founded in 2004", "Est. 1999" pass deterministic validation, weakening the factual-provenance rule ahead of QA-A. Fix shape: broaden the founding-year pattern (careful with false positives on addresses/phone numbers — restrict to word-boundary `est(ablished)?\.?/founded( in)? + year` forms); tripwire QA-F3.
- **F4 (Low, verified, deferred) — #12 JSON-LD parse-only.** `JSON.parse("null")`/`"123"` pass `INVALID_JSON_LD`. Semantic truthfulness remains covered by the QA-B `TRUTHFUL_JSON_LD` mandatory gate at release time, so impact is bounded; fix is a one-line object-shape check.

## 5. Severity summary

Critical 0 · High 0 · Medium 3 (F1–F3, all verified, all small bounded fixes) · Low 1 (F4, deferred).

## 6. Regression notes

No regressions found: the full 166-test suite passes with all prior suites intact on the contracted tree. The one flaky-looking probe result during the pass (global table count racing cross-suite writes) was a probe-construction artifact, replaced with a deterministic DB-statement spy. F1 suggests missing edge-case thinking at the mode-gate boundary (guard placed at one entry point, not the Build-start invariant); worth a gotcha entry when fixed.

## 7. Deferred issues

F4 (JSON-LD object-shape check) — bounded, semantic backstop exists. All standing CSO watch items (M1–M3, L2–L3, W1–W17) remain tracked in `docs/security/`; not re-tested here (security scope).

## 8. Final verdict

**FAILED / NEEDS REWORK — narrowly scoped.** Thirteen of fourteen issues pass every acceptance criterion. #24 fails one criterion in the letter (F1: revision-path gate bypass), and the same bounded rework pass should carry F2 and F3 (both integrity gaps in deterministic gates). No finding blocks the REFERENCE_BOUND automated pipeline itself; this verdict reflects the acceptance-criterion violation, not broad breakage.

## 9. Next best action

Authorize one bounded fix pass for F1–F3 (+F4 opportunistically): gate check in `createRevisionBuild`, screenshot validation wiring in reference intake, founding-year lint broadening — each with its `it.fails` tripwire flipped to a passing regression test, full gates re-run, and a one-line addendum to this report.
